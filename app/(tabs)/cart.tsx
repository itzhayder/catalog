import CartList from "@/components/CartList";
import { Text, View } from "react-native";

const Cart = () => {
    return (
        <View className="flex-1">
            <CartList />
        </View>
    );
};

export default Cart;
